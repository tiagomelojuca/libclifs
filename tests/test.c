#include <stdio.h>
#include "../src/libclifs.h"

// cmd /c "gcc tests/test.c -o build/a.exe && .\build\a"

int main()
{
    Material concrete;
    concrete.e = 100000;
    concrete.g = 38462;

    return 0;
}